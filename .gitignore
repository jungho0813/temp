# class 파일은 제외
*.class

index.txt

# 확장자가 .a 인 파일 무시
*.a

# 윗 라인에서 확장자가 a인파일은 무시하고 lib.a는 무시하지 않음
!lib.a

# 현재 디렉토리에 있는 TODO 파일은 무시하고 subdir/TODO 처럼 하위디렉토리에 있는 파일은 무시하지않음
/TODO

# doc 폴더 아래에 모든 txt파일 무시(doc/server/test.txt. 파일은 무시하지않음)
doc/*.txt

# doc 폴더 아래의 모든 .pdf 파일 무시(doc/server/1.pdf무시)
doc/**/*.pdf